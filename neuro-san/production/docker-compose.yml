services:
  neurosan:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/app/data        # bewaart beelden + campagne-status buiten de container
    restart: unless-stopped
